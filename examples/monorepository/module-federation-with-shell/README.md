# Monorepository

## Module federation with shell

This is a minimalistic example to demonstrate the usage of a shell with client-side routing to load multiple remote host dynamically.

The main application is under `apps/shell`.
